const array = ["딸기", "귤", "사과"];

// 배열의 길이 가져오기
const arrayLength = array.length;

// 배열의 요소 데이터 처리
for (let i = 0; i < arrayLength; i++) {
  console.log(array[i]); // '딸기','귤','사과' 순서로 출력
}

// for (let i = 0; i < 배열길이; i++) {} - for문 루프 처리
