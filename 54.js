const array1 = [2, 4, 10];
console.log(array1.join()); // '2,4,10' (문자열)

const array2 = ["a", "b", "c"];
console.log(array2.join("")); // 'abc' (문자열)

// 배열.join([결합문자열]*) - 배열 요소를 결합해 문자열 만들기 - 문자열 반환
